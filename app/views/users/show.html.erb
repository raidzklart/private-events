<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<% @user.events.each do |event| %>
  <p> <%= event.location %>  <%= link_to 'Show', event_path(event.id) %></p>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
